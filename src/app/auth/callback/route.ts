import { NextResponse, type NextRequest } from "next/server";
import { createClient } from "@/lib/supabase/server";

export async function GET(request:NextRequest){const url=new URL(request.url);const code=url.searchParams.get("code");let invite=url.searchParams.get("invite");let name=url.searchParams.get("name");const supabase=await createClient();if(code){const {error}=await supabase.auth.exchangeCodeForSession(code);if(error)return NextResponse.redirect(new URL(`/login?error=${encodeURIComponent(error.message)}`,url.origin))}if(!invite||!name){const {data}=await supabase.auth.getUser();invite=invite||data.user?.user_metadata?.pending_invite||null;name=name||data.user?.user_metadata?.signup_name||null}if(invite&&name){const {error}=await supabase.rpc("accept_staff_invite",{p_token:invite,p_name:name});if(error&&error.message!=="invite is invalid or expired")return NextResponse.redirect(new URL(`/login?error=${encodeURIComponent(error.message)}`,url.origin))}return NextResponse.redirect(new URL("/app",url.origin))}
